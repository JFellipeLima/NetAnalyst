const { Router } = require("express")
const { getInfo, addInfo} = require("../controllers/netRoutes")

const router = Router()

router.get("/", (req,res) => {

});
router.post("/", (req,res) => {


})
